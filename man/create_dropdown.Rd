% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dropdown.R
\name{create_dropdown}
\alias{create_dropdown}
\title{Cria um Elemento Dropdown Personalizado com Conteúdo Associado}
\usage{
create_dropdown(
  elementos,
  id,
  params,
  label = "",
  html = FALSE,
  fig_width = 8,
  fig_height = 4,
  dropdown_width = "300px"
)
}
\arguments{
\item{elementos}{Uma lista de elementos de conteúdo a serem exibidos (por exemplo, SVGs ou tags HTML). Cada elemento corresponde a uma opção no dropdown.}

\item{id}{Um identificador único para o dropdown e os contêineres de conteúdo associados.}

\item{params}{Um vetor de valores de parâmetro correspondente a cada elemento em \code{elementos}. Usado como atributo value para cada opção do dropdown.}

\item{label}{Uma string de texto especificando o rótulo exibido acima do dropdown. O padrão é string vazia.}

\item{html}{Lógico; se \code{TRUE}, assume que \code{elementos} já são HTML. Se \code{FALSE}, aplica \code{transformar_svg()} a cada elemento. O padrão é \code{FALSE}.}

\item{fig_width}{Numérico; largura (em polegadas) para conteúdo SVG se \code{html = FALSE}. O padrão é 8.}

\item{fig_height}{Numérico; altura (em polegadas) para conteúdo SVG se \code{html = FALSE}. O padrão é 4.}

\item{dropdown_width}{Caracter; largura CSS para o elemento dropdown (por exemplo, "300px"). O padrão é "300px".}
}
\value{
Uma lista de tags HTML contendo o dropdown, um loader e os elementos de conteúdo associados, pronta para renderização em um app Shiny ou documento HTML.
}
\description{
Esta função gera um elemento HTML dropdown que permite ao usuário selecionar entre várias opções,
cada uma associada a um elemento de conteúdo específico (por exemplo, gráficos SVG). O conteúdo é atualizado dinamicamente
com base na seleção do usuário. A função suporta conteúdo HTML e SVG, permitindo a personalização
da aparência do dropdown e das dimensões do conteúdo.
}
\details{
A função verifica se os comprimentos de \code{elementos} e \code{params} coincidem. Se \code{html = FALSE},
aplica a função \code{transformar_svg()} a cada elemento para convertê-lo em HTML/SVG com as dimensões especificadas.
O dropdown é construído usando tags do pacote \code{htmltools}, e cada opção é vinculada a um elemento de conteúdo que
é mostrado ou ocultado conforme a seleção do usuário.
}
